import styled from 'styled-components';

export const Toasts = styled.div`
    z-index: 2;
    position: fixed;
    display: flex;
    right: 1rem;
    bottom: 1rem;
    flex-direction: column;
    height: auto;
`;
