import styled from 'styled-components';

export const Toasts = styled.div`
    z-index: 4;
    position: fixed;
    display: flex;
    right: 1.6rem;
    top: 1.6rem;
    flex-direction: column;
    height: auto;
`;
